version: '3.8'

services:
  cilantro:
    build: .
    container_name: cilantro-dev
    ports:
      - "8030:8080"
    volumes:
      - ./target/ROOT.war:/usr/local/tomcat/webapps/ROOT.war
    restart: unless-stopped